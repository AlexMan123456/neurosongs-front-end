{
  "compilerOptions": {
    "baseUrl": "./src",
    "paths": {
      "#utils": ["utils"],
      "#components/*": ["components/*"],
      "#firebase-config": ["firebase-config.js"],
      "#api": ["../api.js"]
    }
  }
}