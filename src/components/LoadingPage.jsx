import Loading from "./Loading"

function LoadingPage(){
    return (<section>
        <Loading/>
        <p><strong>Do not close or refresh the page!</strong></p>
    </section>)
}

export default LoadingPage